<phone:PhoneApplicationPage
    x:Class="W7Cord.Views.ChannelPickerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True"
    mc:Ignorable="d"
    d:DesignWidth="480" d:DesignHeight="696">

    <toolkit:TransitionService.NavigationInTransition>
        <toolkit:NavigationInTransition>
            <toolkit:NavigationInTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardIn"/>
            </toolkit:NavigationInTransition.Backward>
            <toolkit:NavigationInTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardIn"/>
            </toolkit:NavigationInTransition.Forward>
        </toolkit:NavigationInTransition>
    </toolkit:TransitionService.NavigationInTransition>
    <toolkit:TransitionService.NavigationOutTransition>
        <toolkit:NavigationOutTransition>
            <toolkit:NavigationOutTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardOut"/>
            </toolkit:NavigationOutTransition.Backward>
            <toolkit:NavigationOutTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardOut"/>
            </toolkit:NavigationOutTransition.Forward>
        </toolkit:NavigationOutTransition>
    </toolkit:TransitionService.NavigationOutTransition>

    <phone:PhoneApplicationPage.Resources>

        <DataTemplate x:Key="TextChannelTemplate">
            <Grid Margin="0,0,0,12" Height="80">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding Name}" 
                               FontSize="28" 
                               FontFamily="Segoe WP SemiLight" 
                               Foreground="White"/>
                    <TextBlock Text="{Binding Topic, TargetNullValue='no topic set'}" 
                               FontSize="14" 
                               Foreground="{StaticResource PhoneSubtleBrush}"
                               Margin="12,-4,0,0"/>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="VoiceChannelTemplate">
            <Grid Margin="0,0,0,12" Height="80">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding Name}" 
                               FontSize="28" 
                               FontFamily="Segoe WP SemiLight" 
                               Foreground="White"/>
                    <TextBlock Text="voice channel" 
                               FontSize="14" 
                               Foreground="{StaticResource PhoneSubtleBrush}"
                               Margin="12,-4,0,0"/>
                </StackPanel>
            </Grid>
        </DataTemplate>


        <Style x:Key="ChannelListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,12,0,0" />
        </Style>
    </phone:PhoneApplicationPage.Resources>

    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <Viewbox Width="110" Margin="12,0,0,0" HorizontalAlignment="Left" CacheMode="BitmapCache">
                <Image Source="/Assets/logo.png"
                       Width="1000"
                       Height="231"
                       Stretch="UniformToFill"
                       Margin="-50,32,0,-16"
                       CacheMode="BitmapCache"/>
            </Viewbox>
            <TextBlock x:Name="GuildNameText" 
                       Text="server name" 
                       Margin="12,0,0,0" 
                       Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>


        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,12">
            <ListBox x:Name="ChannelsList" 
                     SelectionChanged="ChannelsList_SelectionChanged"
                     Style="{StaticResource ChannelListBoxStyle}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,12" Height="80">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" 
                                           FontSize="28" 
                                           FontFamily="Segoe WP SemiLight" 
                                           Foreground="White"/>
                                <TextBlock Text="{Binding Topic, TargetNullValue='voice channel'}" 
                                           FontSize="14" 
                                           Foreground="{StaticResource PhoneSubtleBrush}"
                                           Margin="12,-4,0,0"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>


        <Grid x:Name="LoadingOverlay" Background="#80000000" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="loading channels..." Foreground="White" FontSize="24"/>
            </StackPanel>
        </Grid>
    </Grid>


    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="False">
            <shell:ApplicationBar.Buttons>
                <shell:ApplicationBarIconButton x:Name="RefreshButton"
                                                IconUri="/Assets/refresh.png" 
                                                Text="refresh"
                                                Click="RefreshButton_Click"/>
            </shell:ApplicationBar.Buttons>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>
</phone:PhoneApplicationPage>